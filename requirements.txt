numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
requests>=2.31
pypdf>=4.0            # repli extraction reporting PDF si poppler (pdftotext) absent
# extraction PDF primaire : binaire système poppler → `brew install poppler`
# sourcing Quantalys (optionnel, sous licence) : `pip install playwright && playwright install chromium`
